# Sonnet-Window Revisit — Findings

Per-commit Opus re-review verdicts for the Sonnet-window (~Jun 5 2026) toybox commits. See documentation/sonnet-window-revisit-plan.md.

## Step 1 — Phase R (#239)

| commit | verdict | note |
|--------|---------|------|
| a7ebf84 | OK | R3 Q&A gating (SAFETY-CRITICAL). Re-verified all five invariants against current code. (a) The advance gate is a REAL server-side invariant: `post_advance` (src/toybox/api/activities.py:3868-3883) raises 409 `{"code":"question_pending"}` when `current_question is not None and current_q_approved is None`. It is placed AFTER `current_row = steps[current_index]` (line 3866) and BEFORE both the pre-G2 linear branch (line 3890) AND the lazy-insert/choice branch (line 3920+). There is only ONE advance endpoint (`/{activity_id}/advance` at line 3736); both linear and branching (`choice_index`) advances flow through it, so the choice path cannot bypass the gate. (b) `post_approve_question` (line 4092) is parent-only: `_: Annotated[Any, Depends(RequireScope({TokenScope.parent}))]`. (c) Fails CLOSED: migration 0023 adds `question_approved INTEGER NULL` (no DEFAULT), and `_fetch_steps` computes `question_pending = question_val is not None and question_approved_val is None` — NULL = pending = blocked, no auto-approve. (d) No bypass: `_fetch_steps` and `post_advance` both SELECT `question, question_approved`; no advance path skips the predicate. (e) Approve path is atomic: single `with conn:` block updates `activity_steps.question_approved` AND `activities.version = version + 1`, then re-reads via `_row_to_response` (which re-runs `_fetch_steps`) and calls `_emit_state` so the WS envelope carries `question_pending=False`. Migration runner (`db/migrations/__init__.py` `discover_migrations`) auto-applies 0023 by version order, so the columns exist at runtime. Integration tests cover gate-block, approve, skip, no-question regression, WS emit, version_conflict, invalid_transition. |
| c6d4816 | OK | R2 spoken-text char limit. Limit values are sane for kids: presets `{0, 50, 100, 150, 250}`, default `150` (src/toybox/core/spoken_text_limit.py — `SPOKEN_TEXT_LIMIT_VALID`/`DEFAULT`). `0` = off; none absurd, default is not zero. ENFORCED at the real TTS production point: `ReadMeButton.handleClick` calls `truncateAtWordBoundary(text, limit)` and passes the result to `speak()` (frontend/src/child/components/ReadMeButton.tsx) — full body stays on screen, only the spoken string is truncated. Word-boundary snap with hard-cut fallback for a single long word is correct. Backend `get_spoken_text_limit` fails closed to 150 on parse error or out-of-set value; `set_spoken_text_limit` raises ValueError (→422) on invalid input. Migration 0022 seeds the row; legacy DBs resolve via the 150 fallback. |
| 222a804 | OK | R1 remove cadence loop + restyle TriggerButton. Behavior change is intentional and nothing load-bearing was dropped. Removal is scoped to the AUTONOMOUS background proposer (`start_cadence_loop`) and the cadence-coupled `start_proposed_ttl_sweep` in `main._metrics_lifespan`, plus the cadence setting module/router/frontend control. The manual proposal-seeding path (TriggerButton → propose endpoint in api/activities.py) is intact and is now the primary seed mechanism (TriggerButton restyled from text-link to full-width primary CTA — pure styling). `cadenceSeconds={0}` on PlayQueueList cleanly disables the fade math. Migration 0012 settings row left in place (no data loss). No trigger/escalation path silently lost. |
| fafb9c5 | OK | R4 activity search GET /api/search + SearchPanel. NO injection: the user query is escaped for LIKE metacharacters (`safe_q = q.replace("\\","\\\\").replace("%","\\%").replace("_","\\_")` with backslash first, correct order) and bound as a parameter `(pattern,)` under `ESCAPE '\\'` — never string-interpolated (src/toybox/api/search.py:138-147). Template search is pure in-memory Python substring (no SQL). Input validated via `Query(min_length=1, max_length=100)`. Response shape (`past_activities`: id/title/template_id/state/created_at; `templates`: id/title/intent) matches exactly what SearchPanel.tsx renders. The pinned-template propose path (`generate(pinned_template_id=...)`) is guarded by `find_template_by_id` with picker fallback + warning if the id is unknown; `ProposeRequest.template_id` is validated as `str | None`. |
| 5ac5860 | OK | Phase R plan (docs only). Soundness check: the plan (documentation/phase-r-plan.md) is fully consistent with what shipped. Every step's description, the per-file change tables, and the gating contract ("block advance when `question IS NOT NULL AND question_approved IS NULL`, new 409 code `question_pending`", parent-only approve, client-side word-boundary truncation, parameterized LIKE search) match the landed code in a7ebf84/c6d4816/222a804/fafb9c5. No contradiction with shipped behavior. |

**Summary verdict:** All five Phase R commits re-reviewed under Opus are OK — no Sonnet error found. The safety-critical R3 Q&A gate (a7ebf84) is a genuine server-side invariant, not a frontend hide: a single advance endpoint gates both linear and branching paths, the approve/skip action is parent-scope-gated, the schema fails closed on NULL with no auto-approve, and the resolution is atomic (version bump + WS emit in one transaction). No code fix applied; the only change in this worktree is this markdown findings document, which cannot affect the already-green test baseline (2323 pytest / 738 vitest).

### Step 1 — Cross-cutting finding (pre-existing, NOT an R3 regression) → issue #244

An adversarial reviewer pass + orchestrator independent verification surfaced a real enforcement gap that R3 did **not** introduce. R3's scope **design is correct**: `post_advance` is `{parent, child}` (activities.py:3742), `post_approve_question` is `{parent}`-only (activities.py:4098), and `TokenScope` defines both `parent` and `child` — i.e. a *child*-scope token can advance/read but must not clear the gate. The gap is that the production **child kiosk currently authenticates with a PARENT-scope token** (`POST /api/auth/parent`); the child-token pairing flow (`POST /api/auth/pair`) is **deferred to Phase D Step 20** and never called in the frontend (documented at `frontend/src/child/api.ts:6-13`). So today the kiosk's own credential *can* call `approve-question` — the gate is enforced by the child-route UI (no approve button) rather than by the credential boundary. Per `security.md` "UI is not a control."

- **Severity:** architectural HIGH / practical LOW for the target ages (4, 6) — exploitation requires manual API calls with the kiosk token; the child route hides the approve button.
- **Secondary (low):** `GET /api/activities/{id}` (`{parent, child}`) renders future template step `body`s for `proposed`/`approved` activities via `_render_template_plan_steps` (preview), visible to any token in pre-running states. The gated `question` field is NOT emitted on that path and lazy-insert (G2) means the next running-step row isn't persisted, so the question itself does not leak. Pre-existing G2.5 preview behavior, not R3.
- **Fix direction:** land Phase D Step 20 (child-scope kiosk token); then `approve-question`'s existing parent-only scope becomes a real credential boundary with **no R3 code change** needed. Tracked in **issue #244**. a7ebf84 remains verdict **OK** because the R3 diff is correct as written.

## Step 2 — Launcher LAN-bind security (#240)

| item | verdict | note |
|------|---------|------|
| src/toybox/core/bind_guard.py | OK | `check_bind_safe(host, *, pin_set)` (lines 41-51) is fail-closed: loopback → return None; else if `pin_set` → return None; else `raise BindGuardError`. No fall-through that returns None on a non-loopback/no-PIN host. Loopback set `_LOOPBACK_HOSTS = {127.0.0.1, localhost, ::1}` (line 21) is correct and `0.0.0.0` is deliberately NOT in it — `is_loopback("0.0.0.0")` is False (test_bind_guard.py:49), so a `0.0.0.0` bind without a PIN raises (it exposes the LAN). Stable error code `ErrorCode.lan_bind_requires_pin` set in `BindGuardError.__init__` (line 29) and asserted at test_bind_guard.py:26. Tests cover loopback-v4/v6/localhost pass, `0.0.0.0` raise, external-IP raise, and with-PIN pass (test_bind_guard.py:11-50). |
| src/toybox/main.py (guard invocation) | OK | Guard runs at main.py:1235-1239 — `check_bind_safe(args.host, pin_set=_pin_is_set())` is the FIRST thing after `_parse_args`, BEFORE `create_app()` (1243/1249), BEFORE the `--check` branch (1256) and BEFORE `uvicorn.run` (1263). On `BindGuardError` it prints to stderr and `return 1` (aborts the process; does NOT log-and-continue). Unconditional: no flag (`--smoke`/`--check`/`--port`) skips it. `_pin_is_set()` (main.py:172-187) opens the DB, runs migrations (so the `settings` table always exists even on a fresh boot), then returns `pin_is_set(conn)` → `get_pin_hash(conn) is not None` (pin.py:159-165, 116-127). Missing row → False → guard refuses (fail-closed). If the DB itself cannot be opened, `connect()` raises and the exception is uncaught → process exits non-zero WITHOUT serving — still fail-closed; it never returns True / silently passes. |
| scripts/launch-toybox.ps1 (02f8c76) | OK | `$bindHost = if ($LoopbackOnly -or -not $LanIp) { '127.0.0.1' } else { '0.0.0.0' }` — truly defaults to loopback under `-LoopbackOnly` OR when no LAN IP is detected; only binds `0.0.0.0` when a real LAN IP exists. Passes `--host $bindHost` to `toybox.main` (`uv run python -m toybox.main --host $bindHost --port 8000`). It sets `$env:TOYBOX_LAN_IP` (the Origin allow-list value only) but does NOT seed/set a PIN, does NOT set `TOYBOX_HOST`, and injects no guard-disabling flag/env — so the startup guard runs in the spawned backend regardless. The header comment "LAN bind requires the parent PIN" is documentation of an *enforced* invariant (main.py:1236), not a substitute control; per security.md the real enforcement lives in the backend, and it does. Launcher unchanged since 02f8c76 (`git diff 02f8c76 HEAD` empty). |
| scripts/launch-toybox.cmd (02f8c76) | OK | Two lines: `@echo off` + `powershell -NoProfile -ExecutionPolicy Bypass -File "%~dp0launch-toybox.ps1" %*`. A pure pass-through to the .ps1 with `%*` forwarding flags; no PIN seed, no host/env injection, no guard bypass. (`-ExecutionPolicy Bypass` only lets the .ps1 run; it does not affect the backend's Python startup guard.) Unchanged since 02f8c76. |

**Adversarial bypass result:** No bypass found. `args.host` is a single source — its `--host` default is `os.environ.get("TOYBOX_HOST", DEFAULT_HOST)` (main.py:138), and the SAME `args.host` value is passed to BOTH `check_bind_safe(args.host, ...)` (1236) AND `uvicorn.run(app, host=args.host, ...)` (1263). There is no divergence where the guard checks one host and uvicorn binds another. `TOYBOX_HOST=0.0.0.0` flows through `args.host` and is checked (documented in the main.py module docstring and operator/recovery.md:94). No flag combination, arg ordering, or env var lets the backend bind a non-loopback host while skipping the guard; the only way to allow a `0.0.0.0`/LAN bind is to have a real parent PIN set (`pin_set=True`), which is the intended invariant.

**Summary verdict:** All four items are OK — the launcher is a sound Sonnet-window artifact and the bind guard (which predates the window) is a genuine fail-closed startup invariant, not documentation theater. The single highest-priority concern — a bind-without-PIN bypass — does not exist: the guard runs unconditionally before the app is served, checks the exact host uvicorn binds, fails closed on a missing PIN or unopenable DB, and the launcher/.cmd add no bypass path. No code fix applied; the only change in this worktree is this markdown findings doc, which cannot move the green baseline (2323 pytest / 738 vitest), so the suites were not re-run.

## Step 3 — Phase S kiosk visual + avatar animation (#241)

| commit | verdict | note |
|--------|---------|------|
| 2e78fa4 (plan S) | OK | Phase S plan (documentation/phase-s-plan.md, docs only) is consistent with the shipped code. Both S-step Produces tables, the done-when checklists, and the §8 risk table match the landed diffs. The §8 risk row "OAuth token stale at annotation time" (line 204) explicitly documents the intended degrade contract: "Annotation module treats 401 like any other error — logs warning, returns empty dict; **same pattern as judge fallback**" — confirming the animator's failure-swallowing design is the deliberate plan contract, not a Sonnet slip. **One cosmetic plan miscount:** S1's done-when (lines 160-161) says "all 5 persona_ids and null" / "6 inputs (5 personas + null)", but there are only **4** built-in archetype personas (wizard, princess, detective, periodic_table — `src/toybox/personas/__init__.py:3-4`, schema enum's 5th value is `custom`, which carries no fixed visual identity). The shipped theming.ts correctly maps the 4 real personas + routes `custom`/unknown/null to the fallback. Plan-doc-only overcount with zero functional consequence — not worth a code or doc edit. |
| e50a88e (S1 gradients/theming) | OK | (a) **Mapping is total / no unknown-persona crash.** `gradientForPersona` (theming.ts:30-34) explicit-guards null/undefined/empty-string → `PERSONA_GRADIENT_FALLBACK`, then `PERSONA_GRADIENTS[persona_id] ?? PERSONA_GRADIENT_FALLBACK` (line 33) — any unknown id (e.g. `custom`, `dragon_queen`) returns the warm-idle fallback string, never `undefined`. Verified against the real persona vocabulary: exactly 4 archetypes exist (personas/library/*.json), all 4 mapped to distinct gradients. App.tsx:721 calls `gradientForPersona(activity?.persona_id)` with optional-chaining, so a null activity also resolves to fallback. (b) **Pure/deterministic.** theming.ts is a const `Record` + pure string lookup — no I/O, no Date/random, no external dep; importable anywhere. (c) **No accessibility regression.** StepCard wraps content in `rgba(255,255,255,0.82)` with `borderRadius`/`boxShadow` (StepCard.tsx:486-491) lifting text off the gradient; body color darkened `#222 → #1a1a2e` and the responsive font band was *raised* at the iPad-portrait midpoint (`min(4vw,5vh) → min(4.5vw,5.5vh)`, 3rem cap unchanged) — the 1.5rem→1.2rem floor only affects very-small viewports. Net contrast/legibility improves; nothing hides step text. Tests cover all 4 personas distinct + null/undefined/empty/unknown → fallback, plus an App integration test asserting the root element's `style.background` carries the persona gradient (silent-wiring guard, code-quality §4). |
| c7b19e0 / 2040322 (S2 avatar animations) | OK | **The key review — all four sub-checks pass.** (a) **Capability gate:** the animator deliberately does NOT call `is_capable()`, and the docstring says so candidly ("capability gate is intentionally bypassed"). This is NOT a regression — it **matches the established secondary-Claude-call pattern**: the existing judge call (`get_judge_call` activities.py:169 / `judge_and_persist` judge.py:200 / `schedule_judge_sample` labeled_events.py:504) also gates only on token-presence at the FastAPI dep and swallows all failures at the call site, returning `None`. `is_capable()` is wired at the *primary generation/propose* decision and the `--check` CLI (ai/__main__.py:44), not at every fire-and-forget background call. **Offline-degradation behavior IS preserved**: `annotate_step_animations` (animator.py:481-523) wraps the `client.complete_text_sync` call + JSON parse in a blanket `try/except Exception` → logs WARNING → returns `{}`; the kiosk then falls back to `float` (App.tsx:214-219, PersonaAvatar default). (b) **No SDK reintroduction:** grep of `src/toybox/ai` for `import anthropic` / `from anthropic` / `api_key` / `x-api-key` finds ONLY URL/probe strings (`https://api.anthropic.com/v1/messages`, the TCP probe host). `client.py`'s new `complete_text_sync` (client.py:566-573) merely wraps the existing `_do_complete_text_sync` (OAuth-bearer + urllib via `_post_messages`). Auth stays OAuth-only. (c) **Animator failure is non-fatal to approve:** the annotator runs inline (blocking, NOT fire-and-forget) but its Claude-call path can only return `{}` on any failure (offline/timeout/401/parse all caught). `_annotate_and_persist_step_animations` (activities.py:219-266) no-ops on `sync_client is None` and on empty annotations; integration test `test_approve_no_sync_client_succeeds_without_animation` proves approve returns 200 with no client. The residual unguarded surface is the DB-merge (`_fetch_steps` + `SELECT` + `with conn:` UPDATE) on the same `conn` that just committed the transition — but that is the *same* connection `_row_to_response` uses two lines later (activities.py:2791), so it introduces no new fatal surface beyond what approve already had; the *likely* failure (offline Claude) is caught. (d) **Optimistic concurrency respected:** the annotator runs AFTER `_attempt_transition` and AFTER the `if not ok: raise VersionConflictError` guard (activities.py:2781-2785); a 409 means it never runs (test `test_approve_version_conflict_no_annotation` asserts `stub.calls == []`), and its own writes touch only `activity_steps.metadata_json`, never `activities.version`. Reward-step override to `shine` + unknown-animation filtering + frontend `.avatar-animate-{name}` wiring all covered by 10 unit + 3 integration + 7 PersonaAvatar + 3 App tests. |

**Cross-cutting finding (privacy; NOT an S2 regression) → issue #245:** the animator does not honor `listening_mode == OFFLINE` (parent privacy mode). Orchestrator independently confirmed the wiring: `is_capable()` composes capability from `listening_mode` (`ai/capability.py:120-165`), so the **primary** generate/propose path makes no Claude call in OFFLINE mode — but the S2 animator (and the pre-existing judge call) bypass `is_capable()` and still make an outbound `api.anthropic.com` call, sending step content even when the parent chose OFFLINE. The animator bypassed the gate because `is_capable()` is *async* (network probe) and `post_approve` is *sync* — a real technical reason — but that also dropped the cheap *sync* `listening_mode` privacy check. Whether OFFLINE should mean "no Claude at all" vs. "no primary-loop Claude" is a product/privacy-semantics decision (the shared judge precedent is evidence the current design may intend background calls to proceed), so the SWR did NOT change behavior unilaterally — S2's animator remains **OK** (its failure-degradation is sound). Fix direction (a sync `listening_mode == OFFLINE → return {}` guard at both call sites) and the operator decision are tracked in **#245**.

**Summary verdict:** All three Phase S commits re-reviewed under Opus are OK — no Sonnet error found. The two highest-value risks are both clear: the `anthropic` SDK was NOT re-introduced (auth stays OAuth-bearer + urllib), and an animator failure does NOT break the approve path (the Claude call is fully exception-wrapped → `{}` → kiosk `float` fallback, and approve returns 200 even with no OAuth client, proven by integration test). S1's persona→gradient mapping is total and crash-safe (unknown/null/custom → warm fallback) and the step-card treatment improves rather than regresses readability. The animator's deliberate capability-gate bypass mirrors the established judge-call pattern and preserves offline-degradation behavior; the only residual is a pre-existing cross-cutting gap (OFFLINE-mode policy not honored by background calls) that S2 did not introduce. No code fix applied; the only change in this worktree is this markdown findings doc, which cannot move the green baseline (2323 pytest / 738 vitest), so the suites were not re-run.

## Step 4 — Phase T catalog wire-shape (#242)

| commit | verdict | note |
|--------|---------|------|
| 601e329 (T2 GET /api/catalog) | needs-fix | Endpoint is NOT narrowed: `get_catalog` (src/toybox/api/catalog.py:54-87) iterates ALL of `SUPPORTED_INTENTS`, dedups by id, returns the full list with NO limit/slice/filter/cap (the docstring's "no 20-result cap" is real). `total == len(entries)` holds; `step_count` and `themes` derive directly from `_Template`. BUT the response was **silently missing an element marker**: `CatalogEntry` emitted `id, title, intent, themes, step_count` only — and the authoritative "Elements" discriminator the rest of the codebase uses is a per-step `element_id` (`generator._filter_by_category` catalog rule at generator.py:579-582 is `any(step.element_id is not None ...)`, and the runtime `categorize()` at categorize.ts:64-68 reads `step.element_id`). With no `element_id`/element flag on the wire, T3 had nothing to bucket Elements by. FIX: added `has_element: bool` to `CatalogEntry`, populated `has_element=any(step.element_id is not None for step in tmpl.steps)` (catalog.py) — mirrors the backend's own elements rule. Updated `tools/gen_types_ts.py` + regenerated `frontend/src/shared/types.ts` (codegen verified: the regen produced exactly the hand-edit, `git diff` shows only the +7-line `has_element` addition). Added 2 backend tests (has_element is always bool; ≥1 real element template emits `has_element=true` AND carries no `periodic_table` theme). |
| 45ab1f8 (T3 CatalogPanel + categorizeTemplate) | needs-fix | **Genuine wire-shape drift — the Elements catalog tab was silently empty.** `categorizeTemplate` (categorize.ts:82-91) bucketed Elements via `entry.themes.includes("periodic_table")`, and both the function's own comment (categorize.ts:47-49, 78-80) and the T2 TS-type doc asserted "element templates carry the `periodic_table` theme by convention (set by generator.py)." **This convention does not exist.** `periodic_table` is NOT a member of the `Theme` StrEnum (themes.py:20-44 — the 13 themes are adventure/magic/space/animals/vehicles/food/friendship/pirates/knights/weather/music/silly/feelings), so it can never appear in `recommended_themes` → never in `CatalogEntry.themes`. Verified against a real shipped element template: `noble_gas_party_floaters` (templates/branching/request_play.json) has step `element_id="he-2"` but `recommended_themes=["friendship","silly"]` — no `periodic_table`. Pre-fix consequence: on the **Elements** sub-tab in Browse mode, `categorizeTemplate(entry,"elements")` returned false for every one of the ~30 element templates per intent (`includes("periodic_table")` always false → falls to `categoryFromThemes` → "adventures"), showing **zero element cards**; and those element templates wrongly appeared under **Adventures**. The 10 vitest cases passed only because the fixtures FABRICATED `themes:["periodic_table",...]` (categorize.test.ts:270/275, CatalogPanel.test.tsx ENTRY_ELEMENTS) — the exact code-quality.md §3 "dev codified the invented shape into tests" anti-pattern. FIX: `categorizeTemplate` now keys Elements on `entry.has_element` (the wire flag T2's fix adds); replaced the fabricated `periodic_table`-theme fixtures with realistic element fixtures (`has_element:true` + ordinary themes); added a regression asserting an element template with ordinary themes + no `periodic_table` buckets to Elements (and NOT Adventures/Feelings). categorize.test.ts 12→17, CatalogPanel.test.tsx still 10 green. The other wire fields all round-trip correctly (see field diff below). |

**Field-by-field wire diff (backend emits ↔ frontend reads):**

| field | backend `CatalogEntry` emits | frontend reads | verdict |
|-------|------------------------------|----------------|---------|
| `id` | yes (catalog.py:79) | yes — card key, `propose({template_id: entry.id})` (CatalogPanel.tsx:952,970,1118) | OK |
| `title` | yes (:80) | yes — card title (CatalogPanel.tsx:972) | OK |
| `intent` | yes (:81) | yes — badge + `propose({intent})` (CatalogPanel.tsx:951,976) | OK |
| `themes` | yes (:82) | yes — chip badges, chip-row union, theme filter (CatalogPanel.tsx:978,1082,1089) | OK |
| `step_count` | yes (:83) | yes — "{n} steps" meta (CatalogPanel.tsx:984) | OK |
| `has_element` | **ADDED by fix** (:84) | now read by `categorizeTemplate` for the Elements bucket | OK (was the gap) |
| `total` (`CatalogResponse`) | yes | not read (panel uses `catalog.entries`) | OK — extra backend field, harmless |

No field the panel reads is absent from the backend after the fix; the only previously-unrepresented field the panel NEEDED was the element discriminator, now surfaced as `has_element`. `categorizeTemplate` is **total**: `filterCategory===undefined → true`; else `has_element ? "elements" : categoryFromThemes(themes)` where `categoryFromThemes` always returns `"feelings-friends"` or `"adventures"` — no `undefined` fall-through. The shared TS type is codegen-generated (not hand-maintained) and is now back in sync with the backend model.

**Summary verdict:** T2's endpoint is correctly un-narrowed (full catalog, no cap), but the pair shipped a **real wire-shape drift**: the backend never put an element marker on the wire, so T3's `categorizeTemplate` invented a `periodic_table` theme that the `Theme` enum does not define — silently emptying the Elements browse tab and mis-bucketing every element template into Adventures, with the defect codified by fabricated test fixtures. Fixed surgically by adding `has_element: bool` to `CatalogEntry` (derived from the same `step.element_id` rule the backend's own categorizer uses), propagating it through codegen → shared types → `categorizeTemplate`, and replacing the fabricated fixtures with realistic ones plus a regression. Gates green in this worktree: backend `2321 passed, 6 skipped, 3 xfailed` (+2 new catalog tests), frontend `740 vitest passed` (+5 categorize cases), `npm run typecheck` clean, ruff + mypy clean on changed files, and `gen_types_ts.py` regen reproduces the hand-edited `types.ts` exactly (no codegen drift).

## Step 5 — Phase U/V animations + compare_animate.py (#243)

| commit | verdict | note |
|--------|---------|------|
| a25f4a1 (U1 AnimateDiff wrapper + ToyActionSprite fallback) | OK | **AnimateDiff path is dead/superseded by SVD (Phase V), NOT a live defect.** `src/toybox/image_gen/animate.py` (AnimateLCM wrapper, +379) is offline dev tooling whose output was abandoned ("identity preservation too poor", commit 668c1b7) and overwritten by the V2 SVD batch. U1's frontend change — `ToyActionSprite.tsx` flipped initial `format` `loaded:boolean` → `SpriteFormat="webp"` with a two-stage `onError` fallback (webp→png→hidden) — is correct in isolation, AND V1 (4908131) then correctly *built on it* by demoting the initial format back to `png` and gating the `webp` switch behind `animationend` (so the webp fallback chain only fires in idle steady state, never during the intro). No frontend bug introduced by U1 that V1 inherited; the fallback chain is intact across both commits. |
| 7d12c20 (U2/U3 pipeline fixes) | OK | Dead AnimateDiff tooling (`scripts/batch_animate.py` driver + `animate.py` fixes: dropped invalid `safety_checker` kwargs, added `variant=fp16`/`use_safetensors`/`local_files_only`, fixed `vae.enable_slicing()` deprecation). All offline, not imported by the backend/kiosk; the 140-WebP batch it produced was deprecated. Integration tests (`test_batch_animate.py`, 3 no-GPU tests for dry-run/skip/force) are sound. The `local_files_only=True` add is a correct offline-safety hardening. Superseded, not defective. |
| 4908131 (V1 CSS intro + idle WebP state machine) | OK (with surgical accessibility fix applied) | **PRODUCTION, highest weight — state machine is sound; no stuck/dead state, correct fallback, unmount-safe.** (a) **No stuck/dead state, no render loop.** States `format∈{png,webp,hidden}` × `animatingSlot∈{slot,null}`. `onAnimationEnd`→`animatingSlot=null` (+ `format=webp` iff completed slot was `idle`, read from `animatingSlotRef` not the stale `props.slot` closure — the documented race guard is real and correct); non-idle `onAnimationEnd` does NOT touch `format`, so it cannot re-trigger its own animation → no loop. `hidden`→`return null` is terminal but a later `props.slot` change re-runs the effect (`format` reset to `png`) and recovers. (b) **Missing-idle-webp fallback degrades correctly.** idle: png(intro)→[animationend]→webp→[404]`handleError`(format===webp)→png→[404]→hidden. So a missing `idle.webp` degrades to the static `idle.png`, never a broken image; non-idle never enters webp so a png 404 → hidden directly. Matches plan M1 check #5. (c) **Effect cleanup / strict-mode safe.** The lone `useEffect` does only synchronous `setState` (`animatingSlotRef`, `setAnimatingSlot`, `setFormat`) — no timers, listeners, promises, or subscriptions, so no cleanup is needed and there is no setState-after-unmount path; React removes the img's `onAnimationEnd`/`onError` handlers on unmount; strict-mode double-invoke is idempotent. (d) **Intro→idle transition cannot get stuck in normal operation** — the CSS applies an unconditional `animation:…forwards` to `img[data-animating]`, so `animationend` always fires (~0.3-1.0s) and unlocks webp. **Finding applied as a fix (genuine, in-scope production a11y gap, not a crash):** `ToyActionSprite.module.css` shipped with NO `prefers-reduced-motion` guard, even though the child kiosk already establishes that convention (`ElementCard.tsx:141-143` → `animation:none`). Two problems: motion-sensitive children get the intro with no opt-out (M1 check #6 is literally a sensory-concern gate), AND the obvious future fix (`animation:none`, copying ElementCard) would be a **latent stuck-state bug** — `animation:none` never emits `animationend`, so the idle slot would be stranded on `png` forever and never reach the SVD webp loop. Fix: added a `@media (prefers-reduced-motion: reduce)` block that collapses every intro to a `fadeIn 0.01s forwards` (no perceptible motion) instead of `animation:none`, so `animationend` still fires and the webp transition still completes. Added one regression test pinning the state-machine contract (idle reaches webp **solely** via `animationend`). |
| 241eaf0 (V2 --approach svd batch_animate.py) | OK | Offline dev tooling; svd branch is correct and does not regress the existing approach. **Dispatch is total:** `--approach` is `choices=["animatediff","svd"]` (argparse rejects any other value with a clear error/exit 2 — no silent no-op), `default="animatediff"`, and `_run` branches `if args.approach == "svd": return _run_svd(...)` BEFORE the AnimateDiff imports/job-loop, so the legacy path is untouched. `_run_svd` loads the right model (`StableVideoDiffusionPipeline.from_pretrained(svd_path, …, local_files_only=True)` — the plan-mandated offline guard the `compare_animate.py` reference omitted is correctly added here), enforces `--slot idle` only (errors clearly on any other slot), composites PNG-alpha-over-white before resize, seeds `torch.Generator("cpu").manual_seed(args.seed)` for reproducibility, builds output paths via `pathlib` (`output_root / toy_id / "idle.webp"`), per-job try/except keeps the batch going, and returns non-zero iff any job failed. Dry-run and missing-model/missing-PNG paths all exit cleanly. |
| 668c1b7 + ce7e0a0 (compare_animate.py NEW) | OK | Offline A/B/C comparison harness (run on the dev box to evaluate approaches); not imported by the backend/kiosk — judged "correct + no footguns", not runtime-security. (a) **No subprocess/shell footgun.** The script does not shell out at all — no `subprocess`, no `os.system`, no `shell=True`; option A writes an HTML file, B/C call diffusers pipelines in-process. The only attacker-adjacent surface is `snapshot_download(repo_id, local_dir)` behind an explicit `--download` flag with a hard-coded repo id (`stabilityai/stable-video-diffusion-img2vid-xt`). (b) **Paths are confined and built with `pathlib`.** All paths derive from `_ROOT=Path(__file__).parent.parent` joined with `/` (`_STATIC_DIR`, `_OUT_DIR`, model dirs); per-toy paths use hard-coded `_TEST_TOYS` ids, not CLI input. The only path from a CLI arg is `--svd-path` (option C) which is a *dev-supplied* model dir — `Path(svd_path)` with an `.exists()` guard, no traversal risk in this dev-tooling context. (c) **img2vid defaults are sane/coherent:** B = AnimateDiff v2v, `strength=0.7`, `ip_scale=0.85` (IP-Adapter Plus for identity preservation — exactly the right knob), `guidance_scale=1.0` + `num_inference_steps=8` (correct for the LCM scheduler it loads), fixed `seed=42`, 16 frames @ 8fps; C = SVD `num_frames=16`, `num_inference_steps=25`, `decode_chunk_size=4`, seed 42 — all standard, identity-preserving defaults, none obviously wrong. (d) **F401 follow-up (ce7e0a0) is clean:** removes the now-unused `import io` only; nothing else referenced `io` (the script uses Pillow `.save(out_path, …)` directly, no `io.BytesIO`), so nothing dangles. One non-blocking nit (not fixed — dead tooling): the C path passes `enable_model_cpu_offload()` while V2's `batch_animate.py` does the same, which is fine; harness is a one-off evaluator. |

**M1 iPad-UAT status:** PENDING / deferred — **correctly, not silently marked done.** Phase V's V3 is `Type: operator` (`documentation/phase-v-plan.md:214-227`, issue #237) and carries NO done marker; commit `07f7013` ("checkpoint: V2 wait halt — M1 (iPad UAT) deferred bundle appended to plan") appended the M1 entry to the §Manual UAT section (lines 243-266) as an operator-pending bundled UAT (clears R5+S3+O1-O3 #223, T4 #226, U4 #232 in the same session). This is the expected build-phase wait-step handling for a `Type: operator`/`Type: wait` UAT — a known operator-pending item, not a falsely-closed one. V2 itself (241eaf0) is marked `DONE … code shipped … batch pending operator run` (line 192), which is accurate (the offline SVD batch is an operator content-gen run, not a code gate). I did not mark M1 done.

**Summary verdict:** All five commits re-review as **OK**. The AnimateDiff U1/U2/U3 code is dead/superseded by SVD (correctly noted, not a live defect); the V2 svd batch dispatch is total and offline-safe; `compare_animate.py` has no subprocess/path footgun (it never shells out, paths are `pathlib`-confined, img2vid defaults are coherent, and the F401 cleanup is clean). The highest-weight item — the **production** `ToyActionSprite` state machine — is sound: no stuck/dead state, no render loop, correct missing-webp→png→hidden fallback, and unmount/strict-mode safe (the effect does only synchronous setState). The one genuine production finding is an **accessibility gap with a latent stuck-state trap**: the new `ToyActionSprite.module.css` shipped with no `prefers-reduced-motion` guard despite the kiosk's existing `ElementCard` convention, and the obvious copy-the-convention fix (`animation:none`) would have stranded the idle slot on `.png` forever because the webp transition is gated on `animationend`. Fixed surgically with a reduced-motion block that uses a `0.01s` fade (no perceptible motion, but `animationend` still fires) plus a regression test pinning the event-driven contract. Frontend gates green in this worktree: `741 vitest passed` (+1), `npm run typecheck` clean, `npm run lint` clean. No backend files were touched, so the pytest baseline (2325) is unaffected and was not re-run.
